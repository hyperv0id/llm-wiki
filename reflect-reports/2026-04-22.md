# Reflect Report — 2026-04-22

## SUGGESTED_PAGES: Suggested Pages

### Broken wikilinks (즉시 생성 필요)
- `ingest` — persistent-wiki-pattern.md, source-karpathy-llm-wiki.md에서 `[[ingest]]`로 링크하지만 페이지 없음. LLM Wiki 패턴의 3대 오퍼레이션 중 하나.
- `query` — 동일 2개 페이지에서 `[[query]]`로 링크. 3대 오퍼레이션 중 하나.
- `lint` — 동일 2개 페이지에서 `[[lint]]`로 링크. 3대 오퍼레이션 중 하나.
- `dataview` — obsidian.md, source-karpathy-llm-wiki.md에서 `[[dataview]]`로 링크. Obsidian 핵심 플러그인.

### 반복 언급되지만 페이지 없는 엔티티/컨셉
- `positional-encoding` — transformer.md에서 "핵심 설계" 항목으로 기술. Transformer 이해에 필수적인 기법.
- `gpt` — transformer.md, source-attention에서 "GPT 시리즈"로 반복 언급. Transformer 기반 LLM의 대표 계열.
- `bert` — transformer.md, source-attention에서 "BERT"로 반복 언급. Transformer 인코더 기반 모델의 대표.
- `vision-transformer` — transformer.md, source-attention에서 "ViT"로 언급. Transformer의 CV 확장을 보여주는 핵심 사례.
- `noam-shazeer` — Attention Is All You Need 공저자이자 Transformer 연구의 핵심 인물 (후일 Character.AI 공동 창립). 단, source 1개이므로 우선순위 중.

### 우선순위 낮음 (소스 1개 이하)
- 나머지 공저자 5명 (Niki Parmar, Jakob Uszkoreit, Llion Jones, Aidan N. Gomez, Łukasz Kaiser, Illia Polosukhin) — 추가 소스에서 언급 시 생성.
- `minGPT`, `nanoGPT` — Karpathy 교육용 구현. 별도 소스 ingest 시 생성 권장.

---

## SUGGESTED_SCHEMA: Suggested Schema Updates

### 1. 더미/저품질 소스 처리 정책 추가

현재 스키마에 저품질 소스(더미 텍스트, 빈 파일 등) 처리 규칙이 없어 karpathy-llm-wiki-2 ingest 시 불필요한 source-summary가 생성됨.

```diff
 ## Ingest 워크플로 (엄격)

 소스가 `raw/`에 추가되면 다음 단계를 **순서대로** 수행:

 1. **소스 읽기** — 전체 내용을 완전히 읽는다.

+1.5. **소스 품질 판정** — 소스가 다음 조건에 해당하면 **skip 처리**:
+   - 실질적 내용 50단어 미만 (메타데이터 제외)
+   - "test", "duplicate", "placeholder" 등 더미 표식
+   - 기존 소스와 내용이 90% 이상 중복
+
+   Skip 시:
+   - source-summary 페이지를 생성하되, `status: skipped` 표기
+   - 엔티티/컨셉/기법 페이지를 생성하거나 갱신하지 않음
+   - log.md에 `[YYYY-MM-DD] skip | {source} | 사유` 기록
+   - ingest-report에 skip 사유 명시
```

### 2. frontmatter type 값 정합성

현재 일부 페이지에서 `type: source`를 사용하고 있으나, 스키마에 정의된 허용 값은 `source-summary`임. 기존 페이지의 frontmatter를 `type: source-summary`로 수정 필요.

해당 페이지:
- `wiki/source-attention-is-all-you-need.md` — `type: source` → `type: source-summary`
- `wiki/source-karpathy-llm-wiki.md` — `type: source` → `type: source-summary`

### 3. index.md 카테고리 통합

현재 index.md에 `## Sources`(17행)와 `## Source`(30행)가 별도 섹션으로 존재. 하나의 `## Sources` 섹션으로 통합해야 함.

---

## SUGGESTED_SOURCES: Suggested Sources

> wiki_ratio가 낮은 쿼리: (none) — 직접적 데이터 없음.
> 아래는 현재 wiki 주제 분포의 공백을 기반으로 한 보강 추천.

현재 wiki는 (1) LLM Wiki 패턴론, (2) Transformer 아키텍처에 집중. 두 주제를 연결하는 중간 영역과, 각 주제의 심화 방향이 비어 있음.

### Transformer → LLM 진화 경로
- `"GPT-1 Improving Language Understanding by Generative Pre-Training"` — Transformer decoder 기반 LLM의 시초. transformer → gpt 연결에 필수.
- `"BERT: Pre-training of Deep Bidirectional Transformers"` — Transformer encoder 기반. BERT 페이지 생성의 근거.
- `"Scaling Laws for Neural Language Models Kaplan 2020"` — LLM 스케일링 법칙. Transformer가 왜 LLM의 기반이 되었는지 설명.

### 지식 관리 / RAG 심화
- `"Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks Lewis 2020"` — RAG 원논문. 현재 rag.md가 Karpathy 비판만 담고 있어 원래 제안의 맥락 보강 필요.
- `"Memex As We May Think Vannevar Bush 1945"` — source-karpathy-llm-wiki.md에서 언급된 Memex 비전의 원문.

### Attention 메커니즘 심화
- `"Flash Attention Dao 2022"` — 현대 Transformer 구현의 핵심 최적화. self-attention, multi-head-attention 페이지 보강.

---

## CONTRADICTION_REVIEW: Contradiction Review

**발견된 모순: 없음.**

현재 소스 3개(실질 2개) 사이에 사실적 충돌 없음. 두 소스의 도메인이 다름 (지식 관리 패턴 vs. 신경망 아키텍처).

### 잠재적 긴장 영역 (향후 주시)

1. **RAG 평가** — 현재 rag.md는 Karpathy의 비판적 시각만 반영. RAG 원논문(Lewis et al.)을 ingest하면 RAG의 가치에 대해 소스 간 시각차가 발생할 가능성 높음. Contradiction Resolution Case 2 (disputed) 적용 대비.

2. **Transformer 기원** — 현재 "Attention Is All You Need = Transformer 최초 제안"으로 기술. 향후 Bahdanau attention(2014) 등 선행 연구를 ingest하면 "self-attention의 기원" 주장에 뉘앙스 조정이 필요할 수 있음.

### 정책 보완 제안

현재 contradiction 정책은 **사실적 충돌**에 초점. 향후 소스가 늘면 **시각/프레이밍 충돌** (같은 사실을 다르게 평가)도 빈번해질 것. 다음 규칙 추가를 권장:

```
### Case 4: 시각/프레이밍 충돌 (사실은 동일, 평가가 상이)

본문에 `## Perspectives` 섹션을 만들어 양쪽 시각을 병기.
status는 `active` 유지 (사실적 모순이 아니므로 disputed 아님).
```
